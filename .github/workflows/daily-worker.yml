name: Daily Growth Cron

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes
  workflow_dispatch:

jobs:
  run-cron:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger growth cron
        run: |
          curl -X POST "https://cropeye-api.onrender.com/internal/run-daily-cron"
